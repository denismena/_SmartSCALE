<UserControl x:Class="SmartSCALE.View.Nomenclatoare.NomenclatoareListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartSCALE.View.Nomenclatoare"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBlock Text="Lista de nomenclatoare"></TextBlock>
        <StackPanel>
            <!--<Button Content="Furnizor" Command="{Binding NavToNomenclator}" CommandParameter="furnizor"/>
            <Button Content="Client" Command="{Binding NavToNomenclator}" CommandParameter="client"/>
            <Button Content="Operator" Command="{Binding NavToNomenclator}" CommandParameter="operator"/>-->
        </StackPanel>
    </Grid>
</UserControl>
